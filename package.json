{
  "name": "reverie",
  "version": "0.0.2",
  "description": "An experiment in online worlds.",
  "repository": {
    "type": "git",
    "url": "https://github.com/jorsi/reverie.git"
  },
  "main": "index.js",
  "scripts": {
    "start": "npm run build && npm run serve",
    "prebuild": "npm run clean:dist",
    "build": "npm run tslint && npm run build-ts",
    "postbuild": "npm run copy:static && npm run bundle-js",
    "build-ts": "tsc",
    "bundle-js": "browserify ./dist/client/client.js > ./dist/public/js/reverie.js",
    "serve": "node dist/index.js",
    "tslint": "tslint -c tslint.json -p tsconfig.json",
    "clean:dist": "node scripts/cleanDist.js",
    "copy:static": "node scripts/copyClient.js"
  },
  "author": "Jonathon Orsi",
  "license": "MIT",
  "dependencies": {
    "@types/socket.io-client": "^1.4.31",
    "express": "^4.16.2",
    "socket.io": "^2.0.3",
    "socket.io-client": "^2.0.3"
  },
  "devDependencies": {
    "@types/express": "^4.0.37",
    "@types/node": "^8.0.45",
    "@types/socket.io": "^1.4.31",
    "browserify": "^14.4.0",
    "nodemon": "^1.12.1",
    "tslint": "^5.7.0",
    "typescript": "^2.5.3"
  }
}
