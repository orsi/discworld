{
  "name": "reverie",
  "version": "0.0.1",
  "description": "An experiment in online worlds.",
  "repository": {
    "type": "git",
    "url": "https://github.com/jorsi/reverie.git"
  },
  "main": "index.js",
  "scripts": {
    "dev": "nodemon --ignore public/js/ --exec \"npm run build:dev\"",
    "build:dev": "npm run build:js && npm start",
    "build:js": "browserify ./src/client/main.js > ./public/js/reverie-client.js",
    "start": "node ./src/index",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jonathon Orsi",
  "license": "ISC",
  "dependencies": {
    "express": "^4.14.1",
    "socket.io": "^1.7.3"
  },
  "devDependencies": {
    "browserify": "^14.1.0",
    "uglifyify": "^3.0.4",
    "nodemon": "^1.11.0"
  }
}
